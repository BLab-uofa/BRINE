As shown in the NIMS-OS paper (https://nimsos-dev.github.io/nimsos/docs/en/introduction.html), we require an EXPInput folder with the files needed as for the input of the robotic system.
<br>Here, we provide the files we implemented to task the SDL "before" every iteration.
